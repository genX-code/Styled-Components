{
  "name": "makeschoool-challenge",
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.4",
    "http-server": "^0.9.0",
    "twitter-proxy": "^2.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "nodemon": "^1.18.9"
  }
}
